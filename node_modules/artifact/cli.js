#!/usr/bin/env node

var artifact = require('./lib/artifact'),
    beautify = require('js-beautify').js_beautify,
    cli = require('cli');

cli.parse();

artifact.execute(cli.args.join(' '), cli.options).then(function(res){
    console.log(res ? res : '');
}, function(err){
    console.log(err);
});