var endpoint = require('../endpoint');

module.exports = function(artifact) {
    artifact.register('resolve', function(options) {
        return endpoint.get([options.name, options.version], {
            type: 'resolved'
        });
    });
};
