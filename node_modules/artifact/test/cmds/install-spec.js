var chai = require('chai'),
    sinon = require("sinon"),
    sinonChai = require("sinon-chai"),
    expect = chai.expect,
    artifact = require('../../lib/artifact'),
    Q = require('q');

describe('lib/cmds/install.js', function() {

    before(function() {
        chai.use(sinonChai);
    });

    it('Registers install task to artifact', function() {
        expect(artifact.cmds).to.have.property('install');
    });

});