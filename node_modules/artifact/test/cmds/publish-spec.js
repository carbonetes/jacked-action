var chai = require('chai'),
    expect = chai.expect,
    artifact = require('../../lib/artifact');

describe('lib/cmds/publish.js', function() {
    it('Registers publish task to artifact', function() {
        expect(artifact.cmds).to.have.property('publish');
    });
});